{
  "name": "@preview-qa/schemas",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@preview-qa/domain": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "^2.0.0"
  }
}
